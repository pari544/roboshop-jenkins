- name: Ansible Playbook to Create all the RoboShop Project Jobs in Jenkins Server.
  hosts: localhost
  roles:
    - jenkins-jobs
  vars:
    folders:
      - APP-SETUP-WITH-ANSIBLE
    pipeline_jobs:
      - { jobName: "roboshop", folderName: "demo", repoName: "roboshop-ansible"}